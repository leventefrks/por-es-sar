<div class="hero">
  <div class="intro-text">
    <h2 class="title">Az anti városnéző füzet</h2>
    <p class="text">
      „Tudd meg, elődeinknek mi minden nem tetszett hazánkban – gyengéd irónia
      és maró gúny, örökös elégedetlenkedés és folytonos pocskondiázás!”
    </p>
    <a href="/" class="cta">Tovább</a>
  </div>
  <div class="jumbotron">
    <img src="/hero-image.jpg" alt="por és sár" />
  </div>
</div>

<style lang="scss">
  .hero {
    position: relative;
    display: grid;
    grid-template-columns: 1fr;
    place-items: center;
    padding-left: 7rem;
    gap: 8rem;
    height: 100vh;

    .intro-text {
      max-width: 500px;

      .title {
        margin-bottom: 0.5rem;
        font-size: 32px;
        font-family: var(--font-family-primary);
      }

      .text {
        margin-bottom: 2rem;
        font-family: var(--font-family-secondary);
        font-weight: 300;
        font-size: 24px;
        line-height: 42px;
      }

      .cta {
        width: min-content;
      }
    }

    .jumbotron {
      position: relative;
      width: 100%;
      height: 100vh;

      &:before {
        position: absolute;
        left: 0;
        content: '';
        height: 100%;
        min-width: 100%;
        background: linear-gradient(
          180deg,
          rgba(250, 219, 133, 1) 61%,
          rgba(255, 255, 255, 1) 85%
        );
      }

      img {
        position: absolute;
        top: 50%;
        left: -10%;
        transform: translateY(-50%);
        aspect-ratio: 1.42;
        border-radius: 30px;
        max-width: 500px;
      }
    }
  }

  @media screen and (min-width: 700px) {
    .hero {
      grid-template-columns: 1fr 1fr;
    }
  }
</style>
