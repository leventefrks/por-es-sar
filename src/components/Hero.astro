<section class="hero content" id="hero">
  <article class="intro-text">
    <h2 class="title page-title">Az antivárosnéző füzet</h2>
    <p class="text">
      „Tudd meg, elődeinknek mi minden nem tetszett hazánkban – gyengéd irónia
      és maró gúny, örökös elégedetlenkedés és folytonos pocskondiázás!”
    </p>
    <a href="#features" class="cta">
      Tovább
      <svg
        xmlns="http://www.w3.org/2000/svg"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        stroke-linecap="round"
        stroke-linejoin="round"
        viewBox="0 0 24 24"
      >
        <path d="m9 18 6-6-6-6"></path>
      </svg>
    </a>
  </article>
  <div class="jumbotron">
    <div class="image-wrapper">
      <img
        src="/hero-image-opt.jpg"
        class="hero-image"
        alt="por és sár"
        decoding="async"
      />
    </div>
  </div>
</section>

<style lang="scss" scoped>
  .hero {
    position: relative;
    display: grid;
    gap: 2rem;
    background-color: var(--color-background);
    min-height: 100vh;

    .intro-text {
      display: flex;
      flex-direction: column;
      justify-content: center;
      max-width: 62ch;
      width: 100%;
      padding: 0 1.25rem;
      margin: 6.25rem auto;
      color: var(--color-primary);
      text-align: center;

      .cta {
        display: flex;
        align-items: center;
        justify-content: center;
        width: fit-content;
        gap: 0.5rem;
        font-size: clamp(1.1rem, 1.3rem, 1.5rem);
        margin: 0 auto;

        svg {
          width: min-content;
          height: 25px;
        }
      }

      .title {
        position: relative;
        width: fit-content;
        margin-bottom: 0.5rem;
        font-size: clamp(1.5rem, 1.85rem, 2.5rem);

        &:before {
          display: none;
          content: '';
          position: absolute;
          top: -2.3rem;
          left: -2.8rem;
          width: 44px;
          height: 100%;
          background: url('/rectangle-small--yellow.svg') no-repeat;
        }
      }

      .text {
        margin-bottom: 2rem;
        font-family: var(--font-family-secondary);
        font-size: clamp(1.075rem, 1.375rem, 1.575rem);
        line-height: 36px;
      }
    }

    .jumbotron {
      position: relative;
      min-width: 100%;
      height: 100%;
      padding: 0 1.25rem;
      isolation: isolate;
      z-index: 1;

      &:before {
        position: absolute;
        left: 0;
        content: '';
        height: 100%;
        min-width: 100%;
        background: var(--color-hero-gradient);
      }

      .image-wrapper {
        position: relative;
        top: -2rem;
        border-radius: var(--border-radius-normal);
        max-width: 650px;
        margin: 0 auto;

        &:before {
          display: none;
          position: absolute;
          content: '';
          bottom: -2.7rem;
          right: -2.7rem;
          width: 300px;
          height: 300px;
          z-index: 1;
          background: url('/rectangle-grid--gray.svg') no-repeat;
          animation: verticalMovement 5s ease-in infinite backwards;
          will-change: auto;
        }

        @keyframes verticalMovement {
          0%,
          100% {
            transform: translateX(0.5rem);
          }

          50% {
            transform: translateY(0.5rem);
          }
        }

        .hero-image {
          position: relative;
          aspect-ratio: 1.42;
          border-radius: inherit;
          width: 100%;
          object-fit: cover;
          isolation: isolate;
          z-index: 2;
          background-color: #eee;
        }
      }
    }
  }

  @media screen and (min-width: 1000px) {
    .hero {
      grid-template-columns: 1fr 1fr;
      gap: 4rem;
      place-items: center;

      .intro-text {
        padding-left: 2rem;
        animation: fadeDown 250ms ease-in;
        text-align: left;

        .cta {
          margin: unset;
        }

        .title {
          &:before {
            display: block;
          }
        }
      }

      .jumbotron {
        padding: 0;
        &:before {
          display: block;
        }

        .image-wrapper {
          left: -10%;
          top: 50%;
          transform: translateY(-50%);
          animation: opacity 350ms ease-in;

          &:before {
            display: block;
          }
        }
      }
    }
  }
</style>
